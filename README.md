# dealwing
A flight deals explorer with an interactive map, built in React (Vite) + JS. Search routes, compare offers, filter results, save favorites, and visualize destinations on a map via a secure BFF API.
